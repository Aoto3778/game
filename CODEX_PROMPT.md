# Codex 投入用メインプロンプト（これを1通目に貼る）

---

あなたは Android ゲーム開発を単独で完遂するシニアエンジニアです。
これから **完全新規リポジトリ** に、単体で遊んで面白い Android ゲームを作ります。
「動くだけのプロトタイプ」ではなく、**数十時間遊べる完成品** が目標です。

## 0. 絶対制約（違反したら即やり直し）

- 言語: **Kotlin**。UI は **Jetpack Compose**（View 系・XML レイアウトは使わない）
- **画像・音声アセットを一切使わない**。全ての描画は Compose の `Canvas` / `drawBehind` による
  プロシージャル描画で行う。効果音は `android.media.AudioTrack` による矩形波/三角波の
  リアルタイム合成、または `ToneGenerator` で生成する
- **課金 SDK・広告 SDK・アナリティクス・クラッシュレポータを一切入れない**
- **ネットワーク通信を一切しない**（`INTERNET` パーミッションを AndroidManifest に書かない）
- 依存は Google/JetBrains 公式のみ（compose-bom, kotlinx-serialization, datastore-preferences,
  androidx.activity-compose, junit）。それ以外のサードパーティ製ライブラリは追加禁止
- `minSdk = 26`, `targetSdk = 35`, JDK 17, AGP 8.x, Gradle Version Catalog (`gradle/libs.versions.toml`)
- 難読化なし（`minifyEnabled false`）。debug APK がそのまま配布物

## 1. モジュール構成（これが最重要。必ず守る）

```
:core   … 純粋 Kotlin/JVM モジュール。Android 依存ゼロ。
          ゲームルール・カード・敵AI・乱数・セーブデータのシリアライズ。
          UI から完全に独立し、JUnit だけで全挙動を検証できること。
:sim    … 純粋 Kotlin/JVM。main() を持つ CLI。:core を使って自動対戦を大量に回し、
          バランス統計を標準出力に吐く。
:app    … Compose UI。:core の状態を描画し、入力をイベントとして :core に渡すだけ。
          ゲームルールのロジックを :app に書いてはならない。
```

この分離の目的は **あなた自身がエミュレータ無しで正しさを検証できるようにするため** です。
`./gradlew :core:test :sim:run` はローカルで完結し、これがあなたのフィードバックループになります。

## 2. 作るゲーム

同梱の `DESIGN.md` を仕様書として実装してください。
一言でいうと「**取ったカードが相手の選択肢から消える、ゼロサム・ドラフト式ローグライク・デッキビルダー**」です。

`DESIGN.md` に書いていない細部（具体的なカード名、数値、フレーバーテキスト、
ボスの特殊ルール）は **あなたが設計して埋めてください**。仕様の不足は質問ではなく提案で埋めること。
ただし `DESIGN.md` に明記された数値・ルールを勝手に変える場合は、
`DESIGN.md` 側を編集し、変更理由をコミットメッセージに書くこと。

## 3. 進め方（フェーズ制。各フェーズ終了時に必ずコミット）

各フェーズの完了条件を満たすまで次に進まないこと。
「とりあえず全部書いて最後にビルド」は禁止です。

### Phase 0 — 骨格と CI
- 3 モジュールの Gradle 骨格を作る
- 同梱の `.github/workflows/android.yml` を配置する
- 空の Compose 画面が起動するところまで
- **完了条件**: `./gradlew :core:test :app:assembleDebug` がローカルで成功する

### Phase 1 — ルールエンジン（:core）
- 戦闘の状態機械、カード効果の解決順序、バフ/デバフ、乱数（seed 固定の `SplitMix64` を自作）
- **状態は完全に immutable な `data class` + `reduce(state, action): state` 形式にする**
  （リプレイ・アンドゥ・テストが全部これで楽になる）
- **完了条件**: JUnit テストが 80 本以上あり全て緑。カード効果は 1 効果 1 テスト以上

### Phase 2 — コンテンツ量産（ここでトークンを使い切る）
- カード **90 種以上**（アップグレード版含めて実質 180）
- レリック **40 種以上**
- 敵 **28 種以上**、うちエリート 6、ボス 4
- ボスはそれぞれ「戦闘中ルールそのものを書き換える」固有ギミックを持たせる
  （例: 毎ターン手札が 1 枚裏返る／コスト計算式が変わる／ドラフト権を奪う）
- イベントノード **25 種以上**（分岐と代償のあるもの）
- **完了条件**: 全コンテンツが `:core` のデータとして定義され、
  「未実装キーワードを参照しているカードが 0 件」を検証するテストが通る

### Phase 3 — バランス調整の自動化（:sim）
- ルールベースの Bot を 3 種実装（貪欲型 / 温存型 / シナジー重視型）
- `./gradlew :sim:run --args="--runs 2000 --seed 1"` で
  勝率・平均ターン数・カード別採用率/勝率寄与・詰み率を出力
- **完了条件**: 3 Bot の総合勝率が **35〜60%** のレンジに収まり、
  「勝率寄与が上位 3% / 下位 3% の外れ値カード」が 5 枚以下になるまで
  数値を自動で回して調整すること。調整履歴を `BALANCE.md` に残す

### Phase 4 — UI（:app）
- 画面: タイトル / マップ / 戦闘 / ドラフト / デッキ閲覧 / イベント / 結果 / 設定
- カードは Canvas 描画。**ドラッグでプレイ**、離した位置で対象決定
- アニメーション必須: カードのドロー/破棄、ダメージの数字ポップ、画面シェイク、
  致命打時のヒットストップ（80ms）
- 片手縦持ちで完結すること。全操作が画面下 2/3 に収まること
- **完了条件**: 実機/エミュで 1 ラン最後まで通しでプレイできる

### Phase 5 — 仕上げ
- セーブ/レジューム（アプリを殺しても戦闘途中から復帰）
- デイリーチャレンジ（seed = 日付から生成。同じ日は全員同じ盤面）
- 統計画面、実績 30 個
- 効果音の合成（Phase 0 の制約に従う）、ハプティクス
- ダークテーマ固定、日本語/英語の文言リソース分離
- **完了条件**: `DEFINITION_OF_DONE.md` の全項目にチェックが付く

## 4. 作業規律

- 1 フェーズ = 1 コミット以上。コミットメッセージは日本語で、何を検証したかを書く
- **推測でビルドが通ったと言わない**。必ず `./gradlew` を実行し、出力を根拠にする
- 詰まったら勝手に制約を緩めず、`BLOCKERS.md` に「何が・なぜ・代替案 3 つ」を書いて
  そこだけスキップし、他のフェーズを進めること
- 巨大ファイルを作らない。1 ファイル 400 行を超えたら分割する
- 各フェーズ終了時に、進捗・次にやること・懸念を 5 行以内で報告すること

## 5. 最初にやること

1. `AGENTS.md`, `DESIGN.md`, `DEFINITION_OF_DONE.md` を読む
2. Phase 0〜5 を自分の言葉でタスク分解し、`PLAN.md` に書き出す
3. 私の確認を待たずに Phase 0 から着手する

では始めてください。
