# DEFINITION_OF_DONE.md

エージェントは「完成しました」と言う前に、全項目を実行して結果を貼ること。
チェックは自己申告ではなく **コマンド出力を根拠に** 付ける。

## ビルド・検証

- [ ] `./gradlew :core:test` が緑。テスト数 **150 件以上**
- [ ] `./gradlew :app:assembleDebug` が警告 0 で成功
- [ ] GitHub Actions の両ジョブが緑で、APK が Artifacts に出ている
- [ ] `:core` に `import android.` が 1 件も無い（`grep -r "import android\." core/src` が空）
- [ ] `!!` が 0 件（`grep -rn "!!" --include=*.kt` の結果が説明可能な範囲）

## 決定論

- [ ] 同一 seed + 同一 Action 列 → 同一最終状態、を検証するテストがある
- [ ] `kotlin.random.Random.Default` / `Math.random` の使用箇所が 0

## コンテンツ量

- [ ] カード 90 種以上（うちクラス専用が各 25 種以上）
- [ ] レリック 40 種以上
- [ ] 敵 28 種以上（エリート 6 / ボス 4）
- [ ] イベント 25 種以上
- [ ] 実績 30 件
- [ ] 「未定義キーワードを参照するカードが 0 件」を検証するテストが緑

## バランス

- [ ] `:sim` を 3 Bot × 2000 ラン回し、総合勝率が 35〜60%
- [ ] 昇華度 0 と 20 で勝率に 20pt 以上の差がある
- [ ] 採用率 0% のカードが 0 枚、採用率 90% 超のカードが 3 枚以下
- [ ] `BALANCE.md` に、最低 3 回の調整サイクル（統計 → 変更 → 再計測）の記録がある

## プレイ体験

- [ ] 実機またはエミュレータで 3 幕クリアまで通しプレイできる
- [ ] アプリを強制終了 → 再起動で戦闘途中から復帰できる
- [ ] ドラフト画面で「敵に渡した場合の使われ方」が全カードに表示される
- [ ] 敵の次行動が常に見えている
- [ ] 全操作が縦持ち片手で完結する
- [ ] 初回起動時のガイドだけで、説明書無しにルールが理解できる
- [ ] アニメーション速度設定が効く
- [ ] 端末を回転してもクラッシュしない（縦固定でも可、ただし明示すること）
- [ ] 戻るボタンで意図しないランのロストが起きない

## 面白さ（ここを飛ばさない）

- [ ] `:sim` のログから「ドラフトで自分に弱いカードを敵に渡さないために取る」判断が
      **実際に勝率を上げている** ことを数値で示せる（denial を無効化した Bot と比較する）
- [ ] 上記が示せない場合、コアコンセプトが機能していないので設計をやり直すこと

## 配布

- [ ] `README.md` に、APK の入手方法とインストール手順が書かれている
- [ ] ライセンス（MIT 等）が入っている
- [ ] スクリーンショット代わりに、UI の ASCII レイアウト図が README にある
